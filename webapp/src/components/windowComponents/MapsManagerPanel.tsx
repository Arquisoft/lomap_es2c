import React from 'react'
import {AddPlace} from '../mapComponents/AddPlaceComponent'

export default function Pr() {
    return (
        AddPlace(null,null)
    );
  }

export const MapsManagerPanel = () => {
    return (
        <Pr/>
    )
}
